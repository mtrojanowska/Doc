<h1>Edit paragraph</h1>
<%= form_with model: @paragraph do |f| %>
<%= f.hidden_field :section_id, value: params[:section_id]%>

<%= f.label :content %>
<%= f.rich_text_area :body%>

<%= f.label :add_image %>
<%= f.file_field :images, multiple: true %>

<%= f.submit %>
<% end %>